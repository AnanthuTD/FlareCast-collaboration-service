{
  "extends": "./tsconfig.json",
  "exclude": ["node_modules", "test", "dist", "**/*spec.ts"],
  "noEmitOnError": false, // This allows build even if there are type errors
  "skipLibCheck": true, // Skips type checking of all declaration files (*.d.ts)
  "strict": false
}
